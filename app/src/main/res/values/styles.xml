<resources>

    <!--
     Icons: https://icons8.com/icon/set/undefined/color
     -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="BasicCard">
        <item name="cardCornerRadius">@dimen/corners</item>
        <item name="android:layout_margin">@dimen/padding_double</item>
        <item name="android:elevation">10dp</item>
    </style>

    <style name="MediumTextDark">
        <item name="android:fontFamily">@font/baloo2_medium</item>
        <item name="android:lineHeight">18dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/darkgrey</item>
    </style>

    <style name="SmallTextDark">
        <item name="android:fontFamily">@font/baloo2_regular</item>
        <item name="android:lineHeight">16dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/darkgrey</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
